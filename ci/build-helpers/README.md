## Fork publication freeze

This branch retains the build-helper publications for local verification, but configures no remote
publication repository and registers no Maven Central upload task.

## Publish as a local library
```
./gradlew publishToMavenLocal
```
